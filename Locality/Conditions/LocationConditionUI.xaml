<UserControl x:Class="Locality.Conditions.LocationConditionUI"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             xmlns:System="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel Orientation="Vertical" Background="White">
        <StackPanel Orientation="Horizontal">
            <CheckBox IsChecked="{Binding Space.Parameters[location-enable]}" VerticalAlignment="Center" Margin="0,5,0,0" />
            <Label Content="Near a location" FontSize="18" FontWeight="Light" />
        </StackPanel>
        <StackPanel Orientation="Horizontal" Margin="10" Visibility="{Binding Space.Parameters[location-enable], Converter={StaticResource BoolToVisibility}}">
            <!--Label Content="{Binding Space.Parameters[location-name]}" /-->
            <Button Content="Choose" Name="SelectLocation" Click="SelectLocation_Click" Style="{DynamicResource SquareButtonStyle}" />
            <Label Content=", within" />
            <ComboBox SelectedValue="{Binding Space.Parameters[location-distance]}" IsEditable="False">
                <ComboBox.Items>
                    <System:Double>10</System:Double>
                    <System:Double>100</System:Double>
                    <System:Double>1000</System:Double>
                </ComboBox.Items>
            </ComboBox>
            <Label Content="m" />
        </StackPanel>
    </StackPanel>
</UserControl>
